//item model
export class ItemModel {
    constructor(itemId, itemName, price, qtv) {
        this.itemId = itemId;
        this.itemName = itemName;
        this.price = price;
        this.qtv = qtv;
    }
}